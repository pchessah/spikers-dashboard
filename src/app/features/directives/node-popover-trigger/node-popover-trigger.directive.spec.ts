import { NodePopoverTriggerDirective } from './node-popover-trigger.directive';

describe('NodePopoverTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NodePopoverTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
